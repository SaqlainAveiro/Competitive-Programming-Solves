#include<stdio.h>
#include<string.h>
#include<math.h>


int main()
{
    char a[10000];
    int l,n,i,j,c,s,k,count;
    scanf("%d",&n);
    getchar();
    for(i=0;i<n;i++)
    {
        gets(a);
        l=strlen(a);
        char b[l];
        k=sqrt(l);
        if(k*k==l)
        {
            for()
            {

            }
            printf(b);
        }
        else printf("INVALID\n");
    }
}
