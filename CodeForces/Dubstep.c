#include<stdio.h>
#include<string.h>

int main()
{
    char a[200],b[200];
    int i,j,k,n;
    gets(a);
    for(i=0;a[i]!='\0';i=i+3)
    {
        if(a[i]!='W'&&a[i+1]!='U'&&a[i+2]!='B')
        {

        }
    }
}
