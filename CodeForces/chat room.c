#include<stdio.h>
#include<string.h>

int main()
{
    char a[100];
    gets(a);
    int i,j,count=0;
    for(i=0;a[i]!='\0';i++)
    {
        if(a[i]=='h')
            count++;
        if(a[i+1]==)
    }
    if(count==5)
        printf("YES");
    else printf("NO");
}
